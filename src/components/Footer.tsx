import styles from './Footer.module.css'

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <p>
        <span>Todo | React + TS @ 2023</span> 
      </p>
    </footer>
  )
}

export default Footer